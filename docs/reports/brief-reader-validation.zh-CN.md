# 独立只读说明书页验证记录

日期：2026-09-21。执行者为 AI；不是外部真实用户试用。

## 范围

分支 `codex/brief-reader`，基线 `shawn` @ `0b10ae5`。实现提交 `a37f701`；随后补充历史导航草稿保护和本记录，最终 HEAD 以 Git 为准。

新增独立阅读页、固定版本深链接、版本选择、快照内容分区、按版本与哈希匹配的审批展示、手动/focus 刷新及请求失效保护。正文不从当前偏好或约束重建。没有修改后端、数据库或业务写接口。

## 测试证据

| 检查 | 实测结果 |
| --- | --- |
| `uv run --no-sync pytest -q`（临时 UV 缓存） | 245 passed |
| `uv run --no-sync ruff check src tests scripts` | All checks passed |
| `npm test -- --reporter=dot` | 111 passed |
| `npm run build` | TypeScript/Vite 成功 |
| `npm run test:e2e`（最终修复后） | 10 passed，49.6s |

新增纯展示函数测试先由占位实现得到 35 项失败，再实现通过；详情页与 App 导航测试亦经历失败后实现。独立审查发现一处 P2：浏览器后退进入阅读页会丢失未保存输入。新增真实 `history.back()` 单测先失败（未触发确认），统一离开确认后通过；真实浏览器也覆盖取消时保留草稿和再次后退确认。审查代理复核确认该问题已修复，未发现本次修复引入的实质缺陷。

浏览器新增案例使用两个真实认证账户，在工作区生成 v1/v2；分别读取历史与最新版本，验证刷新、前进后退、只读按钮、移动端无横向溢出。监听业务请求确认阅读过程无 POST/PATCH/DELETE，并比较阅读前后 projectState 完全一致。390px 移动截图已查看；截图位于被忽略的 test-results，不提交。

## 已知边界

- 历史审批记录可能已被现有后端清空，显示“历史审批记录不可用”，不能推断当时未批准。
- 不展示模板残留的房型/预算为历史事实；图片证据以来源 ID 展示。
- 不含导出、版本差异比较、真实模型、平面图或三维可视化。
- 离开确认仅覆盖进入阅读页；不是全站草稿持久化。取消浏览器历史导航时重新插入当前工作区 URL，以保留内存草稿。
- 图片分析仍为模拟；本轮验证不代表生产就绪或真实 AI 效果已验证。
- 未合并、未推送、未部署、未调用外部付费模型。
